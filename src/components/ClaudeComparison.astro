---
// Claude Code 直接问 vs Plan 模式对比展示
---

<div class="cc-root">
  <div class="cc-tabs" role="tablist" aria-label="工作模式对比">
    <button class="cc-tab cc-tab--active" id="ccTabDirect" role="tab" aria-selected="true" aria-controls="ccPanelDirect" type="button">
      直接描述
    </button>
    <button class="cc-tab" id="ccTabPlan" role="tab" aria-selected="false" aria-controls="ccPanelPlan" type="button">
      Plan 模式
    </button>
  </div>

  <div class="cc-panels">
    <div class="cc-panel cc-panel--active" id="ccPanelDirect" role="tabpanel" aria-labelledby="ccTabDirect">
      <div class="cc-scenario">需求：给资讯平台加一个"收藏"功能，让用户能保存感兴趣的资讯。</div>
      <div class="cc-convo">
        <div class="cc-bubble cc-bubble--user">
          帮我给资讯平台加一个收藏功能
        </div>
        <div class="cc-bubble cc-bubble--ai">
          好的，我来帮你实现收藏功能。我将：<br/>
          1. 在 news_items 表加一个 is_bookmarked 字段<br/>
          2. 给每张卡片加一个收藏按钮<br/>
          3. 用 localStorage 保存收藏状态<br/>
          4. 加一个"我的收藏"页面<br/><br/>
          <em>（开始改了 8 个文件……）</em>
        </div>
        <div class="cc-bubble cc-bubble--user">
          等等，我不需要"我的收藏"页面，而且我也没想好是存在数据库还是本地……
        </div>
        <div class="cc-bubble cc-bubble--ai">
          抱歉，让我回滚刚才的改动……
        </div>
      </div>
      <div class="cc-tip cc-tip--warn">
        直接描述适合需求清晰的小改动。需求模糊时容易方向跑偏，改完再推倒成本高。
      </div>
    </div>

    <div class="cc-panel" id="ccPanelPlan" role="tabpanel" aria-labelledby="ccTabPlan" hidden>
      <div class="cc-scenario">同一个需求：给资讯平台加一个"收藏"功能，让用户能保存感兴趣的资讯。</div>
      <div class="cc-convo">
        <div class="cc-bubble cc-bubble--user">
          先用 Plan 模式。帮我给资讯平台加一个收藏功能
        </div>
        <div class="cc-bubble cc-bubble--ai cc-bubble--plan">
          <strong>📋 计划草稿（待你确认）</strong><br/><br/>
          在动手之前，有几个关键问题需要你决定：<br/><br/>
          ① 收藏数据存哪里？<br/>
          &nbsp;&nbsp;A. localStorage（无需登录，换设备会丢）<br/>
          &nbsp;&nbsp;B. Supabase（需要用户登录，跨设备同步）<br/><br/>
          ② 需要"我的收藏"专属页面，还是只在首页加一个筛选 Tab？<br/><br/>
          ③ 现有项目没有用户登录系统，如果选 B 需要先做登录——是否要一起做？<br/><br/>
          请告诉我你的选择，我再列出具体文件改动清单。
        </div>
        <div class="cc-bubble cc-bubble--user">
          选 A（localStorage），只加筛选 Tab，不做登录
        </div>
        <div class="cc-bubble cc-bubble--ai">
          明白了。只需改 3 个文件：<br/>
          · NewsCard.tsx — 加收藏按钮，点击写 localStorage<br/>
          · HomePage.tsx — 加"我的收藏"Tab，读 localStorage 筛选<br/>
          · types.ts — 加 BookmarkState 类型<br/><br/>
          确认后我开始动手。
        </div>
      </div>
      <div class="cc-tip cc-tip--good">
        Plan 模式在动手之前对齐需求，适合新功能、不确定影响范围的改动。
      </div>
    </div>
  </div>
</div>

<style>
  .cc-root {
    margin: var(--space-6) 0;
    border: 0.5px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--color-bg-primary);
  }

  .cc-tabs {
    display: flex;
    border-bottom: 0.5px solid var(--color-border);
    background: var(--color-bg-secondary);
  }

  .cc-tab {
    flex: 1;
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    color: var(--color-text-tertiary);
    background: none;
    border: none;
    cursor: pointer;
    border-bottom: 2px solid transparent;
    transition: color 0.2s, border-color 0.2s;
  }

  .cc-tab--active,
  .cc-tab:hover {
    color: var(--color-text-info);
    border-bottom-color: var(--color-text-info);
  }

  .cc-panels { padding: var(--space-4); }

  .cc-panel { display: flex; flex-direction: column; gap: var(--space-3); }

  .cc-scenario {
    font-size: var(--text-xs);
    color: var(--color-text-tertiary);
    padding: var(--space-2) var(--space-3);
    background: var(--color-bg-secondary);
    border-radius: var(--radius-md);
    border-left: 3px solid var(--color-border-secondary);
    line-height: 1.6;
  }

  .cc-convo {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .cc-bubble {
    max-width: 88%;
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-lg);
    font-size: var(--text-sm);
    line-height: 1.65;
  }

  .cc-bubble--user {
    align-self: flex-end;
    background: var(--color-bg-info);
    color: var(--color-text-info);
    border-bottom-right-radius: var(--radius-sm);
  }

  .cc-bubble--ai {
    align-self: flex-start;
    background: var(--color-bg-secondary);
    color: var(--color-text-primary);
    border-bottom-left-radius: var(--radius-sm);
  }

  .cc-bubble--plan {
    border-left: 3px solid var(--color-text-warning);
    background: var(--color-bg-warning);
  }

  .cc-bubble em {
    color: var(--color-text-tertiary);
    font-style: italic;
  }

  .cc-tip {
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-md);
    font-size: var(--text-xs);
    line-height: 1.65;
  }

  .cc-tip--warn {
    background: var(--color-bg-danger);
    color: var(--color-text-danger);
  }

  .cc-tip--good {
    background: var(--color-bg-success);
    color: var(--color-text-success);
  }
</style>

<script>
  (function () {
    var tabDirect = document.getElementById('ccTabDirect');
    var tabPlan   = document.getElementById('ccTabPlan');
    var panelDirect = document.getElementById('ccPanelDirect');
    var panelPlan   = document.getElementById('ccPanelPlan');

    if (!tabDirect || !tabPlan || !panelDirect || !panelPlan) return;

    tabDirect.addEventListener('click', function () {
      tabDirect.className = 'cc-tab cc-tab--active';
      tabPlan.className   = 'cc-tab';
      tabDirect.setAttribute('aria-selected', 'true');
      tabPlan.setAttribute('aria-selected', 'false');
      panelDirect.hidden = false;
      panelPlan.hidden   = true;
    });

    tabPlan.addEventListener('click', function () {
      tabPlan.className   = 'cc-tab cc-tab--active';
      tabDirect.className = 'cc-tab';
      tabPlan.setAttribute('aria-selected', 'true');
      tabDirect.setAttribute('aria-selected', 'false');
      panelPlan.hidden   = false;
      panelDirect.hidden = true;
    });
  })();
</script>
