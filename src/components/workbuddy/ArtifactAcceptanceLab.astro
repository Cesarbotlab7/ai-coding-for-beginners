---
import WorkBuddyShell from './WorkBuddyShell.astro';
---
<section class="wb-lab" data-acceptance-lab>
  <header class="wb-lab__header">
    <span class="wb-lab__eyebrow">互动 08</span>
    <h2 class="wb-lab__title">打开右侧产物，找出“已完成”里的问题</h2>
    <p class="wb-lab__description">AI 停止执行不等于产物合格。先看真实任务页中的对话和产物，再逐项验收。</p>
  </header>

  <WorkBuddyShell
    view="task"
    taskTitle="把会议记录整理成正式纪要"
    workspace="WorkBuddy练习/第一次任务"
    permission="默认权限"
    artifactEmpty={false}
  >
    <div slot="conversation" class="review-conversation">
      <article class="review-user-message">
        请把 meeting-notes.txt 整理成 Markdown 会议纪要，保留原文日期，并列出负责人、截止时间和保存位置。
      </article>
      <article class="review-agent-message">
        <span class="review-agent-mark" aria-hidden="true">W</span>
        <div>
          <p>任务已完成，会议纪要已经生成。</p>
          <div class="review-response-tools" aria-hidden="true">□　♡　♧　⋯　 Auto · 8秒</div>
        </div>
      </article>
    </div>

    <article slot="artifact" class="flawed-artifact">
      <div class="flawed-file-name">
        <span aria-hidden="true">T</span>
        <strong>meeting-summary.txt</strong>
      </div>
      <div class="flawed-preview">
        <p class="flawed-heading">会议纪要</p>
        <p><strong>结论</strong></p>
        <p>新版帮助中心下周上线。</p>
        <p><strong>待办</strong></p>
        <p>尽快完成校对。</p>
        <p><strong>保存位置</strong></p>
        <p>未说明。</p>
      </div>
    </article>
  </WorkBuddyShell>

  <fieldset class="review-list">
    <legend>对照原始要求，你发现了哪些问题？</legend>
    <label><input type="checkbox" value="format" /> 文件格式不是要求的 Markdown</label>
    <label><input type="checkbox" value="date" /> “下周”不是原记录里的“周五”</label>
    <label><input type="checkbox" value="owner" /> 待办缺少负责人和截止时间</label>
    <label><input type="checkbox" value="path" /> 没有说明保存位置</label>
  </fieldset>

  <p class="wb-status" aria-live="polite" data-review-status>已发现 0 / 4 个问题。</p>
  <div class="repair-prompt" hidden data-repair>
    <h3>可以这样要求它修正</h3>
    <pre><code>请根据原始会议记录重新检查产物：
1. 保存为 meeting-summary.md
2. 不要把“周五”改写成“下周”
3. 每个行动项标明负责人和截止时间
4. 回复最终保存路径
修正后逐项自检，并把核对结果列出来。</code></pre>
  </div>
</section>

<script>
  document.querySelectorAll<HTMLElement>('[data-acceptance-lab]').forEach((root) => {
    const checks = [...root.querySelectorAll<HTMLInputElement>('input[type="checkbox"]')];
    const status = root.querySelector<HTMLElement>('[data-review-status]');
    const repair = root.querySelector<HTMLElement>('[data-repair]');
    const render = () => {
      if (!status || !repair) return;
      const completed = checks.filter((check) => check.checked).length;
      status.textContent = completed === checks.length
        ? '验收完成：你找出了全部问题。'
        : `已发现 ${completed} / ${checks.length} 个问题。`;
      status.className = `wb-status ${completed === checks.length ? 'success' : ''}`;
      repair.hidden = completed !== checks.length;
    };
    checks.forEach((check) => check.addEventListener('change', render));
    render();
  });
</script>

<style>
  .review-conversation {
    display: grid;
    gap: var(--space-5);
  }

  .review-user-message {
    max-width: var(--wb-message-width);
    justify-self: end;
    padding: var(--space-3);
    border-radius: var(--wb-message-radius);
    background: var(--wb-client-selected);
    font-size: var(--wb-client-text-size);
  }

  .review-agent-message {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: var(--space-3);
  }

  .review-agent-mark {
    display: inline-flex;
    inline-size: var(--wb-profile-size);
    block-size: var(--wb-profile-size);
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-pill);
    background: var(--wb-client-accent);
    color: var(--wb-client-on-accent);
    font-weight: var(--font-medium);
  }

  .review-agent-message p {
    margin: var(--space-1) 0 var(--space-4);
    font-size: var(--wb-client-text-size);
  }

  .review-response-tools {
    color: var(--wb-client-subtle);
    font-size: var(--wb-client-small-text);
  }

  .flawed-file-name {
    display: flex;
    gap: var(--space-2);
    align-items: center;
    padding-block-end: var(--space-3);
    border-block-end: var(--border-hairline) solid var(--wb-client-divider);
  }

  .flawed-preview {
    margin-block-start: var(--space-3);
    padding: var(--space-3);
    border: var(--border-hairline) solid var(--wb-client-border);
    border-radius: var(--wb-control-radius);
  }

  .flawed-preview p {
    margin: 0 0 var(--space-2);
    font-size: var(--wb-client-small-text);
  }

  .flawed-preview .flawed-heading {
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
  }

  .review-list {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-3);
    margin-block-start: var(--space-5);
    padding: var(--space-4);
    border: var(--border-hairline) solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .review-list legend {
    padding: 0 var(--space-2);
    font-weight: var(--font-medium);
  }

  .review-list label {
    display: flex;
    gap: var(--space-3);
    align-items: flex-start;
  }

  .review-list input {
    inline-size: var(--space-5);
    block-size: var(--space-5);
    flex: 0 0 auto;
  }

  .repair-prompt {
    margin-top: var(--space-5);
  }

  .repair-prompt h3 {
    margin-bottom: var(--space-3);
    font-size: var(--text-base);
  }

  @media (max-width: 40rem) {
    .review-list {
      grid-template-columns: 1fr;
    }
  }
</style>
