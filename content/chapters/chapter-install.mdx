---
slug: install-claude-code
title: "装好 Claude Code，跑第一条命令"
stage: 2
order: 7
summary: "概念看完了，现在把工具装到你的电脑上。四步：装 Node.js、装 Claude Code、拿 API Key、启动。"
updated: 2026-04-26
estimated_reading_minutes: 8
components:
  - InstallGuide
  - ChapterCloser
---

## 从看到做，就差这一步

前面几章，你已经搞清楚了一件事：Claude Code 是一个跑在终端里的 AI 助手，它能读写你电脑上的文件、帮你写代码、修 bug。

但那都是"知道"。

"知道"和"用上"之间，隔着四个步骤。这一章我们一步步走完。

---

## 你需要准备什么

四样东西：

**一台 Mac 或 Windows 电脑**——Linux 也行，但这个教程以 Mac 为主。

**一个可用的账号**——Claude.ai Pro 订阅（$20/月）或 Anthropic API 充值账号，二选一。Claude Code 没有免费通道，这是绕不开的成本。

**5 分钟**——认真的，就这么快。

**能访问国际网络的环境**——Anthropic 的服务器在境外，安装 Claude Code、注册账号、以及日常使用，都需要能正常访问国际网络。这个教程默认你已经有可用的梯子。如果还没有，先解决这一步，否则后面每一步都会卡住。

---

## 什么是「终端」

安装过程需要用到「终端」。终端不是什么神秘工具，它就是一个让你**用文字命令控制电脑**的窗口。

你平时用鼠标点击来做事，终端是用打字来做同样的事。

**Mac 打开终端**：按 ⌘ + 空格，输入「终端」，回车。或者在 Launchpad 里找「终端」。

看到一个黑色或白色的窗口，里面有个光标在闪，就对了。

---

## 四个步骤，全部带模拟演示

下面的组件会带你走完全部四步。每一步都有「模拟运行」按钮——点一下，你能看到命令执行后输出的样子，心里有数再去真的跑。

<InstallGuide />

---

## 装好之后，能做什么

Claude Code 启动之后，你可以直接用中文跟它说话：

> 帮我创建一个 HTML 页面，显示"Hello World"，字体大一点，居中

它会在当前文件夹里创建文件，你用浏览器打开就能看到。

这就是你的第一个「用 AI 写的程序」。

下一章，我们深入看 Claude Code 的工作方式——它是怎么理解你的需求的，什么时候用 Plan 模式，什么时候直接说就够了。

<ChapterCloser />
